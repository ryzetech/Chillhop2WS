name: makezip

on:
  push:
    branches: [ main ]
    paths:
      - server/**
    
  workflow_dispatch:

jobs:
  makezip:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2
       
      - name: release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            server
